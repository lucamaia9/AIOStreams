# Archived Scripts

These scripts are preserved for reference but are NOT part of the active pipeline.

## Why archived

- Superseded by newer scripts (e.g., `promote_bitmagnet_live_to_compact.py`)
- Historical backfill tools replaced by rebuild-live workflow
- Legacy matching scripts replaced by canonical layer + TMDBMemoryIndex
- Test scripts that validated old implementations

## Active scripts

See `scripts/bitmagnet/` for the current active script set.

## Archive date

2026-04-05 — Master plan cleanup Phase 3.
